apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: health-care-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: healthcare.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: health-care-service
            port:
              number: 80
